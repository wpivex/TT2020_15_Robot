/*
#include "lift.hpp"
#include "hamburger.hpp"
*/

// TODO
// Is this file still needed?
/*
Lift::Lift() {
    MotorGroup left({Motor(FOURBAR_LEFT, true, AbstractMotor::gearset::red, AbstractMotor::encoderUnits::degrees)});
    MotorGroup right({Motor(FOURBAR_RIGHT, false, AbstractMotor::gearset::red, AbstractMotor::encoderUnits::degrees)});
    leftMotors = std::make_shared<MotorGroup>(left);
    rightMotors = std::make_shared<MotorGroup>(right);
    leftMotors->setBrakeMode(AbstractMotor::brakeMode::hold);
    rightMotors->setBrakeMode(AbstractMotor::brakeMode::hold);
}

void Lift::setLiftPower() {

}
*/
